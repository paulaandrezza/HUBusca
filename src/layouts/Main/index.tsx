import { SearchBar } from '@/components/SearchBar';
import { MainContainer } from './style';

export const Main = () => {
  return (
    <MainContainer>
      <SearchBar />
    </MainContainer>
  );
};
